sudo: false
language: java
jdk:
  - oraclejdk8
branches:
  only:
    - master
before_install:
    - git clone https://github.com/google/auto.git
    - pushd auto/value
    - mvn install
    - popd
    - git clone https://github.com/Zubnix/jglm.git
    - pushd jglm
    - mvn install
    - popd
install:
    - mvn install
